apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: worker-dev
  namespace: argocd
spec:
  project: app-platform

  source:
    repoURL: https://github.com/mingoo684/cicdex2.git
    targetRevision: test1
    path: k8s/charts/worker
    helm:
      valueFiles:
        - ../../env/dev/worker-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: app-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
