# 목적: 운영 최소 구성
# - (가능하면) auth 활성
# - persistence 활성
# - ClusterIP 내부 전용

auth:
    enabled: true
    password: PROD_REDIS_PASSWORD_CHANGE_ME

persistence:
    enabled: true
    size: 8Gi

service:
    type: ClusterIP
    port: 6379
